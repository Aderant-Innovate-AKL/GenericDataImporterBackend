# pnpm configuration
# Use shamefully-hoist for better compatibility with tools that expect node_modules to be flat
shamefully-hoist=true

# Auto-install peers for convenience
auto-install-peers=true

# Strict peer dependencies (can be disabled if needed)
strict-peer-dependencies=false
